import { NavInner } from '@/components/layout/NavInner';

export function SiteHeader() {
  return (
    <header className="fixed top-0 left-0 z-50 h-16 w-full">
      <NavInner />
    </header>
  );
}
